package com.backend.StockLinker.Message_Service.enums;

/**
 * Type of message content. Text-only per product scope —
 * enum exists for forward compatibility, not to enable other types.
 */
public enum MessageType {
    TEXT
}